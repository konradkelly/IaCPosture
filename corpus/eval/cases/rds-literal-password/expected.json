{
  "description": "RDS master password as a string literal in the resource",
  "category": "hardcoded-secrets",
  "expected": [
    {
      "source": "checkov",
      "rule_id": "CKV_SECRET_10"
    }
  ],
  "note": "Relabelled 2026-09-12 from CKV_SECRET_6. Reading checkov's SECRET_TYPE_TO_ID: 6 is the Base64 High Entropy plugin, 10 is Secret Keyword, and `password = \"...\"` is a keyword match. Was a predicted miss while the scanner ran --framework terraform alone; the secrets framework is on now, so this is expected to HIT."
}
