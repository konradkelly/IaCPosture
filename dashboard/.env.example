# Point at the deployed review-api HTTP API base URL (no trailing slash).
# After `terraform apply`:
#   terraform output -raw review_api_endpoint
VITE_API_BASE_URL=https://xxxxxxxx.execute-api.us-east-1.amazonaws.com
